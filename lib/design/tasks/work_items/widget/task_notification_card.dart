import 'package:flutter/material.dart';

class TaskNotificationCard extends StatelessWidget {
  const TaskNotificationCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}
